name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          
      - name: Run tests
        run: |
          cd backend
          PYTHONPATH=. pytest tests/ -v
        env:
          DATABASE_URL: sqlite:///./test.db

  # Vercel and Render will auto-deploy via their GitHub integrations
  deploy-notification:
    needs: test-backend
    runs-on: ubuntu-latest
    steps:
      - name: Notify deployment
        run: |
          echo "✅ Tests passed!"
          echo "Vercel will auto-deploy frontend"
          echo "Render will auto-deploy backend"
